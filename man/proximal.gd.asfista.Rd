% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rbrm.r
\name{proximal.gd.asfista}
\alias{proximal.gd.asfista}
\title{Adaptive Step-Size FISTA Proximal Gradient Descent for Alpha and Beta}
\usage{
proximal.gd.asfista(
  alpha,
  beta,
  last_y,
  opt,
  step_size,
  lambda,
  t_old,
  intercept,
  va,
  vb,
  x,
  y,
  max_backtrack = 10,
  backtrack_factor = 0.8,
  beta_increase = 1.1,
  prob_fun = getProbRR.org
)
}
\arguments{
\item{alpha}{A numeric vector of alpha coefficients.}

\item{beta}{A numeric vector of beta coefficients.}

\item{opt}{A character string indicating the parameter to optimize; either "alpha" or "beta".}

\item{step_size}{A numeric value for the initial step size. This will be adjusted adaptively.}

\item{lambda}{A numeric value for the L1 regularization parameter.}

\item{t_old}{A numeric value for the previous t parameter in FISTA (used for momentum).}

\item{intercept}{A logical indicating whether to maintain the intercept term.}

\item{va, }{vb, x, y Data or model components passed to the objective function.}

\item{max_backtrack}{Integer, maximum number of backtracking iterations for adaptive step size.}

\item{backtrack_factor}{A numeric factor by which the step size is reduced when backtracking.}

\item{beta_increase}{A numeric factor by which the step size is increased if backtracking succeeds.}

\item{last_value}{A numeric vector representing the coefficients from the previous iteration.}
}
\value{
A list with the following components:
\item{value_new}{A numeric vector of updated coefficients for the specified parameter (alpha or beta).}
\item{t_value}{Updated t parameter for the next FISTA iteration.}
\item{y_value}{A numeric vector of coefficients adjusted with FISTA momentum for the next iteration.}
\item{step_size}{The adapted step size for use in subsequent iterations.}
}
\description{
This function applies the FISTA (Fast Iterative Shrinkage-Thresholding Algorithm)
for optimizing either \code{alpha} or \code{beta} parameters using proximal gradient descent
with adaptive step size via backtracking line search.
}
\details{
The adaptive step size dynamically adjusts during each iteration to improve convergence
by using backtracking line search. When a step size produces a sufficient objective decrease,
it is increased in future iterations to accelerate convergence.

The algorithm performs a backtracking line search to find an adaptive step size that
satisfies the Armijo condition (sufficient decrease condition), ensuring stable convergence.
If the condition is met, the step size is increased by a factor (\code{beta_increase})
to speed up future iterations. If not, the step size is decreased progressively
(by \code{backtrack_factor}) until a satisfactory objective decrease is achieved.
}
