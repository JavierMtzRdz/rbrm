% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv.rbrm2.r
\name{validate_cv_inputs}
\alias{validate_cv_inputs}
\title{Refit a Cross-Validated RBRM Model (with Fragile Auto-Retrieval)}
\usage{
validate_cv_inputs(va, vb, x, y, nfolds, type.measure)
}
\arguments{
\item{object}{A fitted object of class \code{cv_rbrm2}.}

\item{newdata_va}{New validation matrix for alpha predictors. Required.}

\item{newdata_vb}{New validation matrix for beta predictors. Required.}

\item{newdata_x}{New treatment assignment vector. Required.}

\item{newdata_y}{New outcome vector. Required.}

\item{lambda}{Optional. Lambda for refitting (numeric, "lambda.min", "lambda.1se", or NULL for default).}

\item{only_active_vars}{Logical (default \code{FALSE}). Refit using only original active variables?}

\item{refit_lambda_on_active}{Logical (default \code{FALSE}). If \code{only_active_vars=TRUE}, apply lambda penalty to active set? (Default is lambda=0).}

\item{relax_lsso}{Logical or NA (default \code{NA}). Control relaxation (NA uses original setting).}

\item{relax_factor}{Optional numeric (0-1). Factor for relaxation.}

\item{implt}{Optional. The underlying model implementation function. If NULL, attempts (fragile) retrieval from original call. \strong{Explicit passing recommended.}}

\item{prob_fun}{Optional. The probability function. If NULL, attempts (fragile) retrieval/determination. \strong{Explicit passing recommended.}}

\item{opt_fun}{Optional. The optimization function used by implt. If NULL, attempts (fragile) retrieval from original call. \strong{Explicit passing recommended.}}

\item{...}{Additional arguments passed to the \code{implt} function during refitting.}
}
\value{
A list of class \code{refit_cv_rbrm} with refitted results.
}
\description{
Refits a model based on a fitted \code{cv_rbrm2} object. Attempts to
automatically retrieve fitting functions from the original call if not provided,
but \strong{this is fragile and explicit passing is recommended.}
}
