% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv.rbrm.r
\name{complete_relax_lasso}
\alias{complete_relax_lasso}
\title{Complete the relax lasss step on cv_rbrm}
\usage{
complete_relax_lasso(
  va,
  vb,
  x,
  y,
  initial_full_fit,
  lambda_selected,
  relax_lsso,
  relax_factor,
  relax_factors_grid,
  fold_ids,
  implt,
  prob_fun,
  type.measure,
  nfolds,
  p_a,
  p_b,
  ...
)
}
\arguments{
\item{va}{Full validation matrix for alpha.}

\item{vb}{Full validation matrix for beta.}

\item{x}{Treatment assignment vector.}

\item{y}{Outcome vector.}

\item{initial_full_fit}{The fitted model object from the initial Lasso fit on the full data.}

\item{lambda_selected}{The selected lambda value from the initial cross-validation.}

\item{relax_lsso}{Logical indicating whether to perform relaxed Lasso.}

\item{relax_factor}{Optional numeric (0-1). The relaxation factor (gamma). If NULL, it is cross-validated.}

\item{relax_factors_grid}{A numeric vector of relax factors to try during cross-validation (if \code{relax_factor = NULL}).}

\item{fold_ids}{A vector indicating fold membership for each observation (used if \code{relax_factor = NULL}).}

\item{implt}{The underlying model fitting function (e.g., \code{rbrm}).}

\item{prob_fun}{Function to calculate probabilities.}

\item{type.measure}{The metric to optimize during relax factor cross-validation ("deviance", "auc", "accuracy", "sensitivity", "specificity",
"precision", "f1_score").}

\item{nfolds}{Number of folds for relax factor cross-validation (if \code{relax_factor = NULL}).}

\item{p_a}{Number of alpha predictors in the full model.}

\item{p_b}{Number of beta predictors in the full model.}

\item{...}{Additional arguments passed to \code{implt}.}
}
\value{
A list containing information about the relaxed Lasso step, including:
\itemize{
\item \code{performed}: Logical indicating if relaxed Lasso was performed.
\item \code{factor_used}: The relaxation factor used.
\item \code{factor_source}: How the factor was determined ("Provided" or "CV").
\item \code{factor_cv_results}: If CV was used, the results of the cross-validation.
\item \code{reconstruction_status}: Status of coefficient reconstruction ("Success" or failure reason).
\item \code{final_fit}: The fitted model object from the relaxed Lasso (or the initial fit if relaxation failed or was skipped).
}
}
\description{
This internal function is called by \code{cv_rbrm} to perform the relaxed Lasso
step if \code{relax_lsso = TRUE}. It handles the selection of the relax factor
(either by cross-validation or using a provided value) and fits the
relaxed Lasso model on the full dataset using the active set of variables
identified in the initial Lasso fit.
}
\keyword{internal}
