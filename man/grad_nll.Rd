% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gradients.R
\name{grad_nll}
\alias{grad_nll}
\title{Gradient of the Negative Log-Likelihood (Unified Interface)}
\usage{
grad_nll(alpha, beta, X, Y, va, vb, prob_fun, opt = c("alpha", "beta"))
}
\arguments{
\item{alpha}{A numeric vector of coefficients for the treatment model.}

\item{beta}{A numeric vector of coefficients for the control model.}

\item{X}{A numeric vector indicating treatment status (1 = treated, 0 = control).}

\item{Y}{A numeric vector of binary outcomes (1 = event, 0 = no event).}

\item{va}{A numeric matrix of covariates for the treatment model (design matrix for alpha).}

\item{vb}{A numeric matrix of covariates for the control model (design matrix for beta).}

\item{prob_fun}{A function that computes probabilities p0 and p1 based on logrr and logop.  This
function determines the specific parameterization (e.g., Richardson or alternative).
It should return a list with elements \code{p0}, \code{p1}, and \code{class}.}

\item{opt}{A character string indicating which parameters to compute the gradient for.
Must be either "alpha" or "beta".}
}
\description{
This function provides a unified interface to compute the gradient,
dispatching to either \code{.grad_nll_alpha} or \code{.grad_nll_beta} based on the \code{opt} argument.
It's the primary exported function for gradient calculation.
}
