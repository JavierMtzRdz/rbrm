% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gradients.R, R/gradients2.R
\name{grad_nll}
\alias{grad_nll}
\title{Gradient of the Negative Log-Likelihood (Unified Interface)}
\usage{
grad_nll(alpha, beta, y, x, va, vb, prob_fun, opt = "both")

grad_nll(alpha, beta, y, x, va, vb, prob_fun, opt = "both")
}
\arguments{
\item{alpha}{Vector of parameters for theta.}

\item{beta}{Vector of parameters for phi.}

\item{y}{Vector of binary outcomes (Y in paper).}

\item{x}{Vector of binary exposures/treatments (A in paper).}

\item{va}{Design matrix for alpha parameters (W in paper). Rows are observations.}

\item{vb}{Design matrix for beta parameters (Z in paper). Rows are observations.}

\item{prob_fun}{Function that takes theta (logrr), phi (logop) and returns list(p0, p1).}

\item{opt}{Character string specifying which gradient to return ("alpha", "beta", or "both").}
}
\value{
Analytical gradient vector(s) of the negative log-likelihood defined by user's nllh.
}
\description{
This function provides a unified interface to compute the gradient,
dispatching to either \code{.grad_nll_alpha} or \code{.grad_nll_beta} based on the \code{opt} argument.
It's the primary exported function for gradient calculation.
}
